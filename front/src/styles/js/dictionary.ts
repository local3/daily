import themes from "./themes"
import { makeStyles } from "@material-ui/core/styles";

const dictionaryStyles: any = {
}

export default dictionaryStyles

export const useDictionaryStyles = makeStyles({
  ...dictionaryStyles
  }
)